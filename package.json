{
  "name": "mobile-erp",
  "version": "0.1.0",
  "private": true,
  "description": "ERP móvil — monorepo de apps offline-first sobre el platform .NET",
  "scripts": {
    "build:types": "pnpm --filter './packages/**' -r run build",
    "clean": "pnpm -r exec rm -rf node_modules dist .expo",
    "lint": "pnpm -r run lint --if-present"
  },
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=10.0.0"
  },
  "packageManager": "pnpm@10.33.4"
}
